name: "Sample GCP workflow"

on:
  workflow_dispatch:

env:
  PROJECT_ID: bluebirz-playground
  WLID_PROVIDER: projects/390283753510/locations/global/workloadIdentityPools/bluebirz-github-pool-2/providers/github-provider
  SA_EMAIL: github-actions-sa@bluebirz-playground.iam.gserviceaccount.com

permissions:
  id-token: write
  contents: read

jobs:
  prepare:
    name: "prepare"
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          persist-credentials: false

      - id: auth
        name: Authenticate to Google Cloud
        uses: google-github-actions/auth@v2
        with:
          workload_identity_provider: ${{ env.WLID_PROVIDER }}
          project_id: ${{ env.PROJECT_ID }}
          service_account: ${{ env.SA_EMAIL }}

      - name: Setup GCloud
        uses: google-github-actions/setup-gcloud@v2
        with:
          project_id: ${{ env.PROJECT_ID }}

  list-files:
    runs-on: ubuntu-latest
    steps:
      - name: test
        run: >
          gcloud storage ls gs://bluebirz-beam
